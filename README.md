
  # Zirco Programming Language Landing Page

  This is a code bundle for Zirco Programming Language Landing Page.

  ## Running the code

  Run `yarn install` to install the dependencies.

  Run `yarn dev` to start the development server.
  
